[build]
    functions = "netlify/functions"
[functions]
    external_node_modules = ["express", "pg", "sequelize"]
[[redirects]]
    from = "/api/*"
    to = "/.netlify/functions/index/:splat"
    status = 200
    force = true